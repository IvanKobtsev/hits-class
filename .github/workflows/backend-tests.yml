name: Backend Tests

on:
  push:
    branches: [main]
    paths:
      - 'webapi/**'
      - '.github/workflows/backend-tests.yml'
  pull_request:
    branches: [main]
    paths:
      - 'webapi/**'
      - '.github/workflows/backend-tests.yml'

jobs:
  backend-tests:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg17
        ports:
          - 5434:5432
        env:
          POSTGRES_DB: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      integresql:
        image: allaboutapps/integresql:1.0.0
        ports:
          - 5000:5000
        env:
          PGHOST: postgres
          PGUSER: postgres
          PGPASSWORD: postgres
          INTEGRESQL_TEST_MAX_POOL_SIZE: 100

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Node.js scripts dependencies
        run: npm ci
        working-directory: webapi/src/Team13.HitsClass.App/Node.js/Scripts

      - name: Restore dependencies
        run: dotnet restore webapi/Team13.HitsClass.slnx

      - name: Run tests
        run: dotnet test webapi/Team13.HitsClass.slnx --no-restore
        env:
          DOTNET_HOSTBUILDER__RELOADCONFIGONCHANGE: false
